/*
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.6.1/userguide/java_library_plugin.html
 */

apply plugin: 'java'
sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    // Use jcenter for resolving dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // 依存プロジェクトを指定する
    compile project(':common')
}

jar {
    manifest {
        attributes 'Main-Class': 'pj2.Main'

        // 依存ライブラリをクラスパスに指定
        attributes 'Class-Path': configurations.runtime.collect{it.name}.join(' ')
    }

    doLast {
        copy {
            // 依存ライブラリについて、jarのみをコピーする（クラスパスに存在する設定ファイルを除く）
            from configurations.runtime
            into destinationDir
            include '*.jar'
        }
    }
}
